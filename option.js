// Add event listener to all radio inputs, update the index,value,checked in chrome.storage
// const radioInputs = document.querySelectorAll('input[type="radio"]')
// for (let i in radioInputs) {
//     radioInputs[i].addEventListener("change", function () {
//         chrome.storage.sync.get("OptionList", ({ OptionList }) => {
//             OptionList[this.value] = this.checked
//             chrome.storage.sync.set({ OptionList })
//         })
//     })
// }

// input[type="radio"] change event listener
